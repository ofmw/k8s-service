apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend-server
  labels:
    app: web
spec:
  replicas: 3
  selector:
    matchLabels:
      app: web
  template:
    metadata:
      name: frontend-server
      labels:
        app: web
    spec:
      containers:
        - name: web
          image: frontend:2404190206_13
          ports:
            - containerPort: 80